---
phase: 01-foundation-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EventCenter.Web/EventCenter.Web.csproj
  - EventCenter.Web/Program.cs
  - EventCenter.Web/appsettings.json
  - EventCenter.Web/Domain/Entities/Event.cs
  - EventCenter.Web/Domain/Entities/EventAgendaItem.cs
  - EventCenter.Web/Domain/Entities/EventCompany.cs
  - EventCenter.Web/Domain/Entities/Registration.cs
  - EventCenter.Web/Domain/Entities/EventOption.cs
  - EventCenter.Web/Domain/Enums/EventState.cs
  - EventCenter.Web/Domain/Enums/RegistrationType.cs
  - EventCenter.Web/Domain/EventCenterDbContext.cs
  - EventCenter.Web/Data/Configurations/EventConfiguration.cs
  - EventCenter.Web/Data/Configurations/EventAgendaItemConfiguration.cs
  - EventCenter.Web/Data/Configurations/EventCompanyConfiguration.cs
  - EventCenter.Web/Data/Configurations/RegistrationConfiguration.cs
  - EventCenter.Web/Data/Configurations/EventOptionConfiguration.cs
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "Blazor Server project exists and can be built"
    - "Domain entities exist with proper properties for event management"
    - "EF Core DbContext is configured with all entity mappings"
    - "SQL Server connection string is configured"
    - "Database schema can be created via migrations"
  artifacts:
    - path: "EventCenter.Web/EventCenter.Web.csproj"
      provides: ".NET 8 Blazor Server project file"
      contains: "Microsoft.AspNetCore.App"
    - path: "EventCenter.Web/Domain/Entities/Event.cs"
      provides: "Event domain entity"
      min_lines: 20
    - path: "EventCenter.Web/Domain/EventCenterDbContext.cs"
      provides: "EF Core DbContext"
      contains: "DbContext"
    - path: "EventCenter.Web/Data/Configurations/EventConfiguration.cs"
      provides: "EF Core entity configuration"
      contains: "IEntityTypeConfiguration"
  key_links:
    - from: "EventCenter.Web/Program.cs"
      to: "EventCenterDbContext"
      via: "AddDbContext service registration"
      pattern: "AddDbContext<EventCenterDbContext>"
    - from: "EventCenter.Web/Domain/EventCenterDbContext.cs"
      to: "Data/Configurations"
      via: "ApplyConfigurationsFromAssembly"
      pattern: "ApplyConfigurationsFromAssembly"
---

<objective>
Create Blazor Server project structure with domain entities and EF Core configuration for SQL Server.

Purpose: Establish the foundational project structure, domain model, and database infrastructure that all subsequent features will build upon.
Output: A buildable Blazor Server application with complete domain entities ready for authentication and feature development.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Blazor Server project and install packages</name>
  <files>
EventCenter.Web/EventCenter.Web.csproj
EventCenter.Web/Program.cs
EventCenter.Web/appsettings.json
  </files>
  <action>
Create new Blazor Server project using .NET 8 template:
```bash
cd /home/winkler/dev
dotnet new blazorserver -n EventCenter.Web -f net8.0
cd EventCenter.Web
```

Install required NuGet packages for EF Core, SQL Server, and FluentValidation:
```bash
dotnet add package Microsoft.EntityFrameworkCore.SqlServer --version 9.0.*
dotnet add package Microsoft.EntityFrameworkCore.Design --version 9.0.*
dotnet add package FluentValidation --version 11.*
dotnet add package Blazored.FluentValidation --version 2.*
dotnet add package TimeZoneConverter --version 6.*
```

Update appsettings.json to include SQL Server connection string:
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost;Database=EventCenterDb;Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=true"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
```

Verify project builds successfully: `dotnet build`
  </action>
  <verify>
    <automated>dotnet build /home/winkler/dev/EventCenter.Web</automated>
dotnet build returns success (exit code 0), all packages restored, appsettings.json contains ConnectionStrings section
  </verify>
  <done>
Blazor Server project created with .NET 8, all required packages installed, project builds without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create domain entities and enums</name>
  <files>
EventCenter.Web/Domain/Entities/Event.cs
EventCenter.Web/Domain/Entities/EventAgendaItem.cs
EventCenter.Web/Domain/Entities/EventCompany.cs
EventCenter.Web/Domain/Entities/Registration.cs
EventCenter.Web/Domain/Entities/EventOption.cs
EventCenter.Web/Domain/Enums/EventState.cs
EventCenter.Web/Domain/Enums/RegistrationType.cs
  </files>
  <action>
Create Domain/Entities/ and Domain/Enums/ folders within EventCenter.Web.

Create Event.cs entity with properties:
- Id (int, PK)
- Title (string, required, max 200)
- Description (string, max 2000)
- Location (string, max 200)
- StartDateUtc (DateTime)
- EndDateUtc (DateTime)
- RegistrationDeadlineUtc (DateTime)
- MaxCapacity (int)
- MaxCompanions (int)
- IsPublished (bool)
- Navigation: AgendaItems (ICollection<EventAgendaItem>)
- Navigation: Companies (ICollection<EventCompany>)
- Navigation: Registrations (ICollection<Registration>)

Create EventAgendaItem.cs with properties for agenda items including costs for Makler and guests.

Create EventCompany.cs with properties for company invitations and company-specific pricing.

Create Registration.cs with properties for both Makler and company participant registrations (RegistrationType enum: Makler, Guest, CompanyParticipant).

Create EventOption.cs for extra options (add-ons) with pricing.

Create EventState enum (Public, DeadlineReached, Finished) in Domain/Enums/EventState.cs.

Create RegistrationType enum (Makler, Guest, CompanyParticipant) in Domain/Enums/RegistrationType.cs.

All DateTime properties use UTC convention (property names end with "Utc"). All entities use int primary keys. Navigation properties use ICollection<T> for consistency.
  </action>
  <verify>
    <automated>dotnet build /home/winkler/dev/EventCenter.Web</automated>
All entity files exist, all entities have int Id properties, Event entity has at least 3 navigation properties, enums exist with expected values
  </verify>
  <done>
Domain entities created with proper properties, relationships defined, enums created, all files compile
  </done>
</task>

<task type="auto">
  <name>Task 3: Create EF Core DbContext and entity configurations</name>
  <files>
EventCenter.Web/Domain/EventCenterDbContext.cs
EventCenter.Web/Data/Configurations/EventConfiguration.cs
EventCenter.Web/Data/Configurations/EventAgendaItemConfiguration.cs
EventCenter.Web/Data/Configurations/EventCompanyConfiguration.cs
EventCenter.Web/Data/Configurations/RegistrationConfiguration.cs
EventCenter.Web/Data/Configurations/EventOptionConfiguration.cs
EventCenter.Web/Program.cs
  </files>
  <action>
Create EventCenterDbContext.cs in Domain/ folder inheriting from DbContext with:
- DbSet<Event> Events
- DbSet<EventAgendaItem> AgendaItems
- DbSet<EventCompany> EventCompanies
- DbSet<Registration> Registrations
- DbSet<EventOption> EventOptions
- OnModelCreating override calling ApplyConfigurationsFromAssembly

Create Data/Configurations/ folder and implement IEntityTypeConfiguration<T> for each entity:

EventConfiguration.cs:
- Table name "Events"
- Title: Required, MaxLength(200)
- Location: MaxLength(200)
- Description: MaxLength(2000)
- DateTime properties: HasColumnType("datetime2")
- CHECK constraint: RegistrationDeadlineUtc <= StartDateUtc
- Index on IsPublished
- Relationships configured (one-to-many for AgendaItems, Companies, Registrations)

Similar configurations for EventAgendaItem, EventCompany, Registration, and EventOption following EF Core Fluent API best practices from research (max lengths, required fields, indexes, foreign keys).

Register DbContext in Program.cs:
```csharp
builder.Services.AddDbContext<EventCenterDbContext>(options =>
    options.UseSqlServer(
        builder.Configuration.GetConnectionString("DefaultConnection"),
        sqlOptions =>
        {
            sqlOptions.EnableRetryOnFailure(
                maxRetryCount: 5,
                maxRetryDelay: TimeSpan.FromSeconds(30),
                errorNumbersToAdd: null);
            sqlOptions.CommandTimeout(30);
        })
    .EnableSensitiveDataLogging(builder.Environment.IsDevelopment())
    .EnableDetailedErrors(builder.Environment.IsDevelopment()));
```

Verify project builds after adding DbContext registration.
  </action>
  <verify>
    <automated>dotnet build /home/winkler/dev/EventCenter.Web</automated>
All IEntityTypeConfiguration files exist, EventCenterDbContext compiles, Program.cs contains AddDbContext call, dotnet build succeeds
  </verify>
  <done>
EF Core DbContext created with all entity configurations, configurations use IEntityTypeConfiguration pattern, DbContext registered in DI container, project builds successfully
  </done>
</task>

</tasks>

<verification>
Run `dotnet build` in EventCenter.Web directory - must succeed with no errors.
Verify all domain entities exist with proper properties.
Verify DbContext is registered in Program.cs.
Verify appsettings.json contains SQL Server connection string.
</verification>

<success_criteria>
- Blazor Server project exists and builds successfully
- All domain entities (Event, EventAgendaItem, EventCompany, Registration, EventOption) exist with proper properties
- EF Core DbContext configured with IEntityTypeConfiguration pattern
- All required NuGet packages installed
- Project structure follows single-project architecture with Domain/ and Data/ folders
- Ready for EF Core migration creation in next plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-01-SUMMARY.md`
</output>
